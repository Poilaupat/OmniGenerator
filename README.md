# OmniGenerator
OmniGenerator is a tool for generating files in any desired format (text, images, etc) and filled with smart random data.
It was created to help the testing of systems or sub-systems that import external files. It can be used in both functional testing and load testing scenarios.
